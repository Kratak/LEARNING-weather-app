import * as React from 'react'
import { Component } from 'react'
import styled from './../sc/styled-components'
// import Weather from './weather.old'

const FormWrapper = styled.div`
margin : auto;

`

export default class Form extends Component {

	public render() {
		return (
            <FormWrapper>
               {/* <Weather /> */}
               
            </FormWrapper>
        );
	}
}
